name: Deploy Static Kurban Website to GitHub Pages

on:
  push:
    branches:
      - main  

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Repo'yu çek
        uses: actions/checkout@v4

      - name: GitHub Pages için artifact hazırla
        uses: actions/upload-pages-artifact@v3
        with:
          path: .  

      - name: GitHub Pages'e deploy et
        uses: actions/deploy-pages@v4
